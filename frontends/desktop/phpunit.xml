<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <!-- Sem isto, o valor real de DESKTOP_API_BASE_URL do .env deste
             servidor (https://192.168.1.100:8443/api/v1) vaza pros testes,
             e qualquer teste sem Http::preventStrayRequests() acaba batendo
             de verdade no backend real em vez do Http::fake() —
             centenas de testes deste arquivo assumem 127.0.0.1:8000. -->
        <env name="DESKTOP_API_BASE_URL" value="http://127.0.0.1:8000/api/v1"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <!-- Cache de views proprio da suite. Sem isto os testes compilam
             o Blade dentro de storage/framework/views como o usuario de
             desenvolvimento, e o www-data passa a falhar com
             "touch(): Utime failed" — 500 no site inteiro. Mesma linha que
             o backend/phpunit.xml ja tinha. -->
        <env name="VIEW_COMPILED_PATH" value="./.phpunit-storage/framework/views"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
