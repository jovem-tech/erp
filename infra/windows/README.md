# Infraestrutura Windows

Templates para desenvolvimento local com XAMPP.

Arquivos principais:

- `apache-vhost.conf`: virtual host do backend.
- `hosts.example`: entradas de hosts locais para o dominio de desenvolvimento.

